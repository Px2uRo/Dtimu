<Page
    x:Class="Dtimu.Views.MusicListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dtimu.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" xmlns:model="using:Dtimu.Models" xmlns:ctrl="using:Dtimu.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Pivot>
        <PivotItem Header="歌曲">
            <ListBox Name="MusicsLB" SelectionChanged="MusicsLB_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate x:DataType="model:Music">
                        <Grid>
                            <TextBlock FontSize="20" Text="{Binding Title}" />
                            <!--<ctrl:Marquee Text="{Binding Title}"/>-->
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </PivotItem>
        <PivotItem Header="歌手">
            
        </PivotItem>
        <PivotItem Header="专辑">
            
        </PivotItem>
    </Pivot>
</Page>
