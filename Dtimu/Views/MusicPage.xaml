<Page
    x:Class="Dtimu.Views.MusicPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dtimu"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <!-- VisualStateManager -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <!-- 小屏幕状态 -->
                <VisualState x:Name="Narrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!--<Setter Target="MainPanel.Orientation" Value="Vertical"/>
                        <Setter Target="MainPanel.Background" Value="LightBlue"/>-->
                    </VisualState.Setters>
                </VisualState>

                <!-- 大屏幕状态 -->
                <VisualState x:Name="Wide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="768"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!--<Setter Target="MainPanel.Orientation" Value="Horizontal"/>
                        <Setter Target="MainPanel.Background" Value="LightGreen"/>-->
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <StackPanel VerticalAlignment="Center" Width="350" x:Name="MusicInfo_Area">
            <Image x:Name="AlumbCover" Width="300" Height="300"/>
            <TextBlock Text="Title" FontSize="28" FontWeight="Bold" Margin="0,30,0,0"/>
            <TextBlock Text="Author"/>
            <Slider Name="SeekController"/>
            
            <MediaElement x:Name="mediaPlayer" 
                          AreTransportControlsEnabled="False">

            </MediaElement>

        </StackPanel>
        
        
    </Grid>
    
</Page>
