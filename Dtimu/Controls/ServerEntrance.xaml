<UserControl
    x:Class="Dtimu.Controls.ServerEntrance"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dtimu.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Height="60"
    Width="240"
    >
    <Button BorderThickness="0" Click="Button_Click" Padding="0,0,0,0">
        <Border Height="60"
    Width="240" Background="Transparent">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>
                <Border Background="Gray" Grid.Column="0">

                </Border>
                <TextBlock Name="PART_Title" Grid.Column="1" HorizontalAlignment="Left" 
                       VerticalAlignment="Center" FontSize="24" 
                       Margin="10,0,0,0" FontWeight="Bold"/>
            </Grid>
        </Border>
    </Button>
</UserControl>
