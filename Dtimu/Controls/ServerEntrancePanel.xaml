<UserControl
    x:Class="Dtimu.Controls.ServerEntrancePanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dtimu.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" xmlns:ctrl="using:Dtimu.Controls" xmlns:model="using:Dtimu.Models"
    d:DesignHeight="300" 
    d:DesignWidth="400">

    <ItemsControl Name="LV">
        <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="model:ServerInstance">
                <ctrl:ServerEntrance Margin="0,3,0,0"
                Title="{x:Bind HostName}"
                Tag="{x:Bind IPAddress}" />
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
