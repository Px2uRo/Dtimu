<UserControl
    x:Class="Dtimu.Controls.RecentItemPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dtimu.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" xmlns:ctrl="using:Dtimu.Controls"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <ScrollViewer x:ConnectionId='1' 
        Height="240" Name="SCRLV"
        HorizontalScrollMode="Enabled" 
        IsVerticalRailEnabled="False"
        IsHorizontalRailEnabled="True" 
        HorizontalScrollBarVisibility="Visible">
        <ListView x:ConnectionId='2' Name="LV" Height="240">
            <ListView.ItemTemplate>
                <DataTemplate                             >
                    <ctrl:RecentItem Title="{Binding Title}">
                        
                    </ctrl:RecentItem>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Vertical" MaximumRowsOrColumns="1"></ItemsWrapGrid>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </ScrollViewer>
</UserControl>

